# Supply Chain Security: pip configuration
# uv も PIP_INDEX_URL / PIP_EXTRA_INDEX_URL 環境変数を参照する

[global]
# レジストリを公式 PyPI のみに固定（dependency confusion 対策）
index-url = https://pypi.org/simple/
# 追加レジストリを無効化
no-extra-index-url = true
